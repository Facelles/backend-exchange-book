// Barrel export — ensures associations are registered when models are imported
export { default as User } from './User';
export { default as Book } from './Book';
export { default as ExchangeRequest } from './ExchangeRequest';
